<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Artist Dashboard - RangRiti</title>
    <style>
        /* RangRiti Consistent Theme Variables */
        :root {
            --sandalwood-beige: #F5DEB3;
            --warm-sandalwood: #E8D5B7;
            --deep-sandalwood: #DCC49C;
            --ivory: #FFFFF0;
            --cream-white: #FAF0E6;
            --pearl-white: #F8F6F0;
            --chocolate-brown: #3D2914;
            --rich-brown: #6B4423;
            --royal-gold: #DAA520;
            --amber-gold: #FFBF00;
            --bronze-gold: #CD7F32;
            --antique-gold: #CFB53B;
            --warm-glow: rgba(255, 215, 0, 0.1);
            --gold-glow: rgba(218, 165, 32, 0.08);
            --font-luxury: 'Cinzel', serif;
            --font-elegant: 'Playfair Display', serif;
            --font-classic: 'Crimson Text', serif;
        }

        /* Google Fonts Import - RangRiti Standard */
        @import url('https://fonts.googleapis.com/css2?family=Cinzel:wght@400;500;600;700;900&family=Crimson+Text:ital,wght@0,400;0,600;1,400&family=Playfair+Display:wght@400;700&display=swap');

        /* Reset and Base Styles */
        *, *::before, *::after {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            font-size: 16px;
            scroll-behavior: smooth;
            overflow-x: hidden;
        }

        body {
            font-family: var(--font-classic);
            background: 
                linear-gradient(45deg, var(--sandalwood-beige) 0%, var(--warm-sandalwood) 25%, var(--deep-sandalwood) 50%, var(--sandalwood-beige) 75%, var(--warm-sandalwood) 100%),
                radial-gradient(circle at 20% 80%, rgba(255, 215, 0, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 80% 20%, rgba(139, 69, 19, 0.08) 0%, transparent 50%);
            background-size: 400% 400%, 100% 100%, 100% 100%;
            animation: megaDynamicBackground 30s ease infinite;
            min-height: 100vh;
            position: relative;
            color: var(--chocolate-brown);
            line-height: 1.7;
        }

        @keyframes megaDynamicBackground {
            0% { 
                background-position: 0% 50%, 20% 80%, 80% 20%; 
                filter: hue-rotate(0deg) brightness(1) contrast(1);
            }
            25% { 
                background-position: 50% 80%, 40% 60%, 60% 40%; 
                filter: hue-rotate(3deg) brightness(1.02) contrast(1.01);
            }
            50% { 
                background-position: 100% 50%, 80% 20%, 20% 80%; 
                filter: hue-rotate(6deg) brightness(1.01) contrast(1.02);
            }
            75% { 
                background-position: 80% 20%, 60% 40%, 40% 60%; 
                filter: hue-rotate(4deg) brightness(1.03) contrast(1.01);
            }
            100% { 
                background-position: 0% 50%, 20% 80%, 80% 20%; 
                filter: hue-rotate(0deg) brightness(1) contrast(1);
            }
        }

        /* Traditional Paper Texture Overlay */
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: 
                radial-gradient(circle at 25% 25%, transparent 2px, rgba(139, 69, 19, 0.02) 2px),
                radial-gradient(circle at 75% 75%, transparent 2px, rgba(139, 69, 19, 0.02) 2px);
            background-size: 80px 80px;
            opacity: 0.4;
            z-index: -1;
            pointer-events: none;
        }

        /* Header - RangRiti Style */
        .header {
            background: linear-gradient(135deg, var(--chocolate-brown), var(--rich-brown));
            padding: 2rem 0;
            text-align: center;
            position: relative;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(61, 41, 20, 0.3);
        }

        .header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 30% 20%, rgba(255, 215, 0, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 70% 80%, rgba(255, 215, 0, 0.08) 0%, transparent 50%);
            pointer-events: none;
        }

        .header-content {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: relative;
            z-index: 2;
        }

        .header h1 {
            font-family: var(--font-luxury);
            font-size: clamp(2.5rem, 5vw, 4rem);
            color: var(--ivory);
            font-weight: 900;
            text-shadow: 
                3px 3px 6px rgba(0, 0, 0, 0.4),
                0 0 30px rgba(255, 215, 0, 0.3);
            letter-spacing: -0.02em;
        }

        .header-subtitle {
            font-family: var(--font-elegant);
            font-size: clamp(1rem, 2vw, 1.3rem);
            color: var(--cream-white);
            font-weight: 400;
            font-style: italic;
            opacity: 0.95;
            margin-top: 0.5rem;
        }

        .logout-btn {
            padding: 12px 24px;
            background: linear-gradient(135deg, var(--royal-gold), var(--amber-gold));
            color: white;
            border: none;
            border-radius: 25px;
            font-family: var(--font-elegant);
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
            transition: all 0.3s ease;
            box-shadow: 0 8px 25px rgba(218, 165, 32, 0.3);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .logout-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 35px rgba(218, 165, 32, 0.4);
            background: linear-gradient(135deg, var(--bronze-gold), var(--chocolate-brown));
        }

        /* Main Container */
        .main-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 3rem 2rem;
        }

        /* Dashboard Grid */
        .dashboard-grid {
            display: grid;
            grid-template-columns: 1fr 2fr;
            gap: 2rem;
            margin-bottom: 3rem;
        }

        /* Card Base Style - RangRiti Standard */
        .card-base {
            background: linear-gradient(145deg, rgba(255, 255, 255, 0.95), rgba(245, 222, 179, 0.8));
            backdrop-filter: blur(10px);
            border: 2px solid rgba(218, 165, 32, 0.3);
            border-radius: 25px;
            padding: 2rem;
            box-shadow: 0 20px 40px rgba(61, 41, 20, 0.15);
            position: relative;
            overflow: hidden;
        }

        .card-base::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 20% 30%, rgba(255, 215, 0, 0.05) 0%, transparent 40%),
                radial-gradient(circle at 80% 70%, rgba(139, 69, 19, 0.03) 0%, transparent 40%);
            pointer-events: none;
        }

        /* Profile Card */
        .profile-card {
            height: fit-content;
            text-align: center;
            position: relative;
            z-index: 2;
        }

        .profile-image {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            border: 4px solid var(--royal-gold);
            object-fit: cover;
            margin: 0 auto 1.5rem;
            display: block;
            box-shadow: 0 15px 35px rgba(218, 165, 32, 0.3);
        }

        .profile-name {
            font-family: var(--font-luxury);
            font-size: 1.8rem;
            color: var(--chocolate-brown);
            margin-bottom: 0.5rem;
            font-weight: 700;
        }

        .profile-artform {
            color: var(--rich-brown);
            font-size: 1.1rem;
            font-weight: 600;
            margin-bottom: 1rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            font-family: var(--font-elegant);
        }

        .profile-location {
            color: var(--chocolate-brown);
            font-size: 1rem;
            margin-bottom: 1.5rem;
            position: relative;
            z-index: 2;
        }

        .profile-location::before {
            content: "📍 ";
            margin-right: 0.5rem;
        }

        .social-links {
            display: flex;
            justify-content: center;
            gap: 1rem;
            margin-top: 1.5rem;
            position: relative;
            z-index: 2;
        }

        .social-link {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 45px;
            height: 45px;
            background: linear-gradient(135deg, var(--royal-gold), var(--amber-gold));
            color: white;
            border-radius: 50%;
            text-decoration: none;
            font-size: 1.2rem;
            transition: all 0.3s ease;
            box-shadow: 0 8px 20px rgba(218, 165, 32, 0.3);
        }

        .social-link:hover {
            transform: translateY(-3px) scale(1.1);
            box-shadow: 0 15px 30px rgba(218, 165, 32, 0.4);
        }

        /* Bio Section */
        .bio-section {
            position: relative;
            z-index: 2;
        }

        .section-title {
            font-family: var(--font-luxury);
            font-size: 1.5rem;
            color: var(--chocolate-brown);
            margin-bottom: 1.5rem;
            text-align: center;
            position: relative;
            font-weight: 700;
        }

        .section-title::after {
            content: '';
            display: block;
            width: 80px;
            height: 3px;
            background: linear-gradient(to right, transparent, var(--royal-gold), transparent);
            margin: 0.5rem auto 0;
            border-radius: 2px;
        }

        .bio-text {
            color: var(--chocolate-brown);
            font-size: 1.1rem;
            line-height: 1.8;
            text-align: justify;
            padding: 1.5rem;
            background: rgba(255, 255, 255, 0.4);
            border-radius: 15px;
            border-left: 4px solid var(--royal-gold);
            position: relative;
            z-index: 2;
        }

        /* Actions Section */
        .actions-section {
            position: relative;
            z-index: 2;
        }

        /* Featured Actions - Enhanced for RangRiti */
        .featured-actions {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 2rem;
            margin: 2rem 0 3rem 0;
            padding: 2.5rem;
            background: linear-gradient(135deg, var(--sandalwood-beige), var(--warm-sandalwood));
            border-radius: 20px;
            border: 3px solid var(--royal-gold);
            box-shadow: 0 20px 40px rgba(218, 165, 32, 0.2);
            position: relative;
            overflow: hidden;
        }

        .featured-actions::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 30% 30%, rgba(255, 215, 0, 0.08) 0%, transparent 50%),
                radial-gradient(circle at 70% 70%, rgba(218, 165, 32, 0.05) 0%, transparent 50%);
            pointer-events: none;
        }

        .featured-action-btn {
            display: flex;
            align-items: center;
            gap: 1.5rem;
            padding: 2rem;
            background: linear-gradient(135deg, var(--ivory), rgba(255, 248, 231, 0.95));
            border: 2px solid rgba(218, 165, 32, 0.3);
            border-radius: 15px;
            color: var(--chocolate-brown);
            text-decoration: none;
            font-family: var(--font-classic);
            cursor: pointer;
            position: relative;
            overflow: hidden;
            transition: all 0.3s ease;
            box-shadow: 0 10px 25px rgba(218, 165, 32, 0.15);
            min-height: 120px;
            z-index: 2;
        }

        .featured-action-btn:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 40px rgba(218, 165, 32, 0.25);
            background: linear-gradient(135deg, var(--royal-gold), var(--amber-gold));
            color: white;
            border-color: var(--ivory);
        }

        .workshop-btn {
            background: linear-gradient(135deg, var(--ivory), #F5E6A8);
            border-color: var(--amber-gold);
        }

        .workshop-btn:hover {
            background: linear-gradient(135deg, var(--amber-gold), var(--bronze-gold));
        }

        .marketplace-btn {
            background: linear-gradient(135deg, var(--ivory), var(--warm-sandalwood));
            border-color: var(--royal-gold);
        }

        .marketplace-btn:hover {
            background: linear-gradient(135deg, var(--chocolate-brown), var(--rich-brown));
        }

        .featured-icon {
            font-size: 2.5rem;
            flex-shrink: 0;
            background: linear-gradient(135deg, var(--royal-gold), var(--amber-gold));
            width: 70px;
            height: 70px;
            border-radius: 15px;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 8px 20px rgba(218, 165, 32, 0.3);
            border: 2px solid rgba(255, 255, 255, 0.3);
            transition: all 0.3s ease;
        }

        .featured-action-btn:hover .featured-icon {
            background: linear-gradient(135deg, var(--ivory), white);
            transform: scale(1.05);
            box-shadow: 0 12px 25px rgba(255, 255, 255, 0.4);
        }

        .featured-content {
            flex: 1;
            text-align: left;
            position: relative;
            z-index: 2;
        }

        .featured-content h4 {
            font-family: var(--font-luxury);
            font-size: 1.4rem;
            margin-bottom: 0.5rem;
            font-weight: 600;
            color: var(--chocolate-brown);
        }

        .featured-action-btn:hover .featured-content h4 {
            color: white;
        }

        .featured-content p {
            font-size: 1rem;
            opacity: 0.8;
            line-height: 1.4;
            font-weight: 400;
            color: var (--rich-brown);
        }

        .featured-action-btn:hover .featured-content p {
            color: rgba(255, 255, 255, 0.9);
            opacity: 1;
        }

        .featured-arrow {
            font-size: 1.5rem;
            font-weight: normal;
            transition: transform 0.3s ease;
            flex-shrink: 0;
            color: var(--royal-gold);
            background: rgba(218, 165, 32, 0.1);
            width: 40px;
            height: 40px;
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            border: 2px solid rgba(218, 165, 32, 0.2);
        }

        .featured-action-btn:hover .featured-arrow {
            transform: translateX(4px);
            background: rgba(255, 255, 255, 0.9);
            color: var(--chocolate-brown);
            border-color: rgba(255, 255, 255, 0.5);
        }

        /* Regular Actions Grid */
        .actions-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1.5rem;
            margin-top: 1rem;
        }

        .action-btn {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.8rem;
            padding: 1.2rem;
            background: linear-gradient(135deg, var(--ivory), var(--cream-white));
            border: 2px solid rgba(218, 165, 32, 0.2);
            border-radius: 12px;
            color: var(--chocolate-brown);
            text-decoration: none;
            font-family: var(--font-classic);
            font-weight: 600;
            font-size: 1rem;
            transition: all 0.3s ease;
            cursor: pointer;
            position: relative;
            overflow: hidden;
        }

        .action-btn:hover {
            transform: translateY(-2px);
            background: linear-gradient(135deg, var(--royal-gold), var(--amber-gold));
            color: white;
            box-shadow: 0 10px 25px rgba(218, 165, 32, 0.3);
            border-color: var(--ivory);
        }

        .action-icon {
            font-size: 1.3rem;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .header-content {
                flex-direction: column;
                gap: 1rem;
                text-align: center;
            }

            .dashboard-grid {
                grid-template-columns: 1fr;
                gap: 1.5rem;
            }

            .main-container {
                padding: 2rem 1rem;
            }

            .featured-actions {
                grid-template-columns: 1fr;
                padding: 1.5rem;
            }

            .actions-grid {
                grid-template-columns: 1fr;
            }
        }

        @media (max-width: 480px) {
            .featured-action-btn {
                flex-direction: column;
                text-align: center;
                padding: 1.5rem;
                gap: 1rem;
                min-height: auto;
            }

            .featured-arrow {
                transform: rotate(90deg);
            }

            .main-container {
                padding: 1rem 0.5rem;
            }
        }

        /* Smooth entrance animations */
        .card-base {
            animation: elegant-fade-in 0.8s ease-out forwards;
            opacity: 0;
            transform: translateY(20px);
        }

        .bio-section {
            animation-delay: 0.2s;
        }

        .actions-section {
            animation-delay: 0.4s;
        }

        @keyframes elegant-fade-in {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>

<body>
    <!-- Header -->
    <header class="header">
        <div class="header-content">
            <div>
                <h1>RangRiti</h1>
                <div class="header-subtitle">Artist Dashboard</div>
            </div>
            <button class="logout-btn" onclick="logout()">Logout</button>
        </div>
    </header>

    <!-- Main Container -->
    <div class="main-container">
        <!-- Dashboard Grid -->
        <div class="dashboard-grid">
            <!-- Profile Card -->
            <div class="profile-card card-base">
                <img src="<%= artist.profilePictureUrl %>" alt="Artist Profile" class="profile-image" id="profileImage">
                <h2 class="profile-name" id="artistName">
                    <%= artist.firstName + ' ' + artist.lastName %>
                </h2>
                <div class="profile-artform" id="artistArtform">
                    <%= artist.specialization || '—' %>
                </div>
                <div class="profile-location" id="artistLocation">
                    <%= artist.city || '—' %>
                </div>
                <div class="social-links">
                    <a href="#" class="social-link" id="instagramLink" title="Instagram">
                        <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                            <path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/>
                        </svg>
                    </a>
                    <a href="#" class="social-link" id="facebookLink" title="Facebook">
                        <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                            <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
                        </svg>
                    </a>
                    <a href="#" class="social-link" id="xLink" title="X (Twitter)">
                        <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                            <path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/>
                        </svg>
                    </a>
                </div>
            </div>

            <!-- Bio Section -->
            <div class="bio-section card-base">
                <h3 class="section-title">Artistic Journey</h3>
                <div class="bio-text" id="artistBio">
                    <%= artist.bio || 'No bio provided.' %>
                </div>
            </div>
        </div>

        <!-- Actions Section -->
        <div class="actions-section card-base">
            <h3 class="section-title">Quick Actions</h3>
            
            <!-- Featured Actions - Key Features -->
            <div class="featured-actions">
                <button class="featured-action-btn workshop-btn" onclick="window.location.href = '/artist/workshops'">
                    <div class="featured-icon">🎭</div>
                    <div class="featured-content">
                        <h4>Create Workshop</h4>
                        <p>Share your skills with art enthusiasts</p>
                    </div>
                    <div class="featured-arrow">→</div>
                </button>
                
                <button class="featured-action-btn marketplace-btn" onclick="window.location.href = '/marketplace'">
                    <div class="featured-icon">🛍️</div>
                    <div class="featured-content">
                        <h4>Explore Marketplace</h4>
                        <p>Discover and sell traditional artworks</p>
                    </div>
                    <div class="featured-arrow">→</div>
                </button>
            </div>

            <!-- Regular Actions Grid -->
            <div class="actions-grid">
                <button class="action-btn" onclick="window.location.href = '/product'">
                    <span class="action-icon">🎨</span>
                    <span>Upload Artwork</span>
                </button>
                <button class="action-btn" onclick="window.location.href='/artist/edit-profile'">
                    <span class="action-icon">✏️</span>
                    <span>Edit Profile</span>
                </button>
                <button class="action-btn" onclick="window.location.href = 'orders'">
                    <span class="action-icon">📦</span>
                    <span>View Orders</span>
                </button>
                <button class="action-btn" onclick="window.location.href = '/artist/gallery'">
                    <span class="action-icon">🖼️</span>
                    <span>Manage Gallery</span>
                </button>
                <button class="action-btn" onclick="contactSupport()">
                    <span class="action-icon">💬</span>
                    <span>Contact Support</span>
                </button>
            </div>
        </div>
    </div>

    <script>
        const params = new URLSearchParams(window.location.search);
        if (params.get('success') === 'true') {
            alert("✅ Product saved successfully!");
        }
        function logout() {
            let ans = confirm("Do you want to logout ?");
            if (ans) {
                window.location.href = '/logout';
            }
        }
    </script>
</body>

</html>