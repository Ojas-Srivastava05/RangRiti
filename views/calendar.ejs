<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Workshops Calendar - RangRiti</title>
    <!-- FullCalendar CSS -->
    <script src='https://cdn.jsdelivr.net/npm/fullcalendar@6.1.11/index.global.min.js'></script>
    <%- include('partials/navbar-styles') %>
    
    <style>
        /* Google Fonts Import for Traditional Look */
        @import url('https://fonts.googleapis.com/css2?family=Crimson+Text:ital,wght@0,400;0,600;1,400&family=Playfair+Display:wght@400;700&display=swap');

        /* Root Variables - Sandalwood Beige Theme */
        :root {
            --sandalwood-beige: #F5DEB3;
            --sandalwood-light: #FFF8E7;
            --sandalwood-dark: #D2B48C;
            --chocolate-brown: #3D2914;
            --rich-brown: #6B4423;
            --royal-gold: #B8860B;
            --amber-gold: #CD853F;
            --cream-white: #FFFDD0;
            --antique-bronze: #8B4513;
            --font-classic: 'Crimson Text', serif;
            --font-display: 'Playfair Display', serif;
        }

        /* Reset and Base Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: var(--font-classic);
            background: linear-gradient(135deg, var(--sandalwood-beige) 0%, var(--sandalwood-dark) 100%);
            min-height: 100vh;
            margin-top: 80px;
            position: relative;
        }

        /* Traditional Paper Texture Overlay */
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: 
                radial-gradient(circle at 25% 25%, transparent 2px, rgba(139, 69, 19, 0.02) 2px),
                radial-gradient(circle at 75% 75%, transparent 2px, rgba(139, 69, 19, 0.02) 2px);
            background-size: 80px 80px;
            opacity: 0.4;
            z-index: -1;
            pointer-events: none;
        }

        /* Page Title */
        .page-title {
            text-align: center;
            color: var(--chocolate-brown);
            margin: 40px 0;
            font-family: var(--font-display);
            font-size: 3em;
            text-shadow: 2px 2px 4px rgba(61, 41, 20, 0.15);
            position: relative;
        }

        .page-title::after {
            content: '';
            position: absolute;
            bottom: -15px;
            left: 50%;
            width: 150px;
            height: 4px;
            background: linear-gradient(to right, transparent, var(--royal-gold), transparent);
            transform: translateX(-50%);
        }

        /* Calendar Container */
        #calendar {
            max-width: 1200px;
            margin: 0 auto 40px;
            padding: 30px;
            background: linear-gradient(145deg, var(--cream-white), var(--sandalwood-light));
            border-radius: 20px;
            box-shadow: 0 15px 35px rgba(61, 41, 20, 0.15);
            border: 3px solid var(--royal-gold);
            position: relative;
        }

        /* FullCalendar Custom Styling */
        .fc {
            font-family: var(--font-classic) !important;
        }

        .fc-toolbar {
            background: linear-gradient(135deg, var(--chocolate-brown), var(--rich-brown)) !important;
            padding: 15px 20px !important;
            border-radius: 12px !important;
            margin-bottom: 20px !important;
            box-shadow: 0 8px 25px rgba(61, 41, 20, 0.3) !important;
        }

        .fc-toolbar-title {
            color: var(--cream-white) !important;
            font-family: var(--font-display) !important;
            font-size: 1.8em !important;
            font-weight: 700 !important;
        }

        .fc-button {
            background: var(--royal-gold) !important;
            border: 2px solid var(--cream-white) !important;
            color: var(--cream-white) !important;
            font-family: var(--font-classic) !important;
            font-weight: 600 !important;
            border-radius: 8px !important;
            padding: 8px 15px !important;
            transition: all 0.3s ease !important;
        }

        .fc-button:hover {
            background: var(--antique-bronze) !important;
            transform: translateY(-2px) !important;
            box-shadow: 0 6px 15px rgba(61, 41, 20, 0.3) !important;
        }

        .fc-button:disabled {
            background: #999 !important;
            opacity: 0.6 !important;
        }

        .fc-button-active {
            background: var(--chocolate-brown) !important;
            box-shadow: inset 0 3px 8px rgba(0, 0, 0, 0.3) !important;
        }

        /* Calendar Grid */
        .fc-daygrid-day {
            background: var(--cream-white) !important;
            border: 1px solid var(--sandalwood-dark) !important;
            min-height: 120px !important;
        }

        .fc-daygrid-day:hover {
            background: #FFFFFF !important;
        }

        .fc-day-today {
            background: var(--sandalwood-light) !important;
            border: 2px solid var(--royal-gold) !important;
        }

        .fc-col-header-cell {
            background: var (--chocolate-brown) !important;
            color: var(--cream-white) !important;
            font-family: var(--font-display) !important;
            font-weight: 600 !important;
            padding: 12px 0 !important;
            border: 1px solid var(--rich-brown) !important;
        }

        .fc-daygrid-day-number {
            color: var(--chocolate-brown) !important;
            font-weight: 700 !important;
            font-size: 1.2em !important;
            padding: 8px !important;
            text-shadow: 1px 1px 2px rgba(255, 255, 255, 0.5) !important;
        }

        .fc-daygrid-day-top {
            text-align: left !important;
            padding: 5px !important;
        }

        /* Event Styling */
        .fc-event {
            background: linear-gradient(135deg, var(--royal-gold), var(--amber-gold)) !important;
            border: 2px solid var(--chocolate-brown) !important;
            border-radius: 8px !important;
            color: white !important;
            font-family: var(--font-classic) !important;
            font-weight: 600 !important;
            padding: 4px 8px !important;
            margin: 2px !important;
            cursor: pointer !important;
            transition: all 0.3s ease !important;
            box-shadow: 0 2px 8px rgba(139, 69, 19, 0.3) !important;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.5) !important;
        }

        .fc-event:hover {
            transform: scale(1.02) !important;
            box-shadow: 0 4px 15px rgba(139, 69, 19, 0.5) !important;
            background: linear-gradient(135deg, var(--antique-bronze), var(--chocolate-brown)) !important;
        }

        .fc-event-title {
            font-weight: 700 !important;
            font-size: 0.85em !important;
            color: white !important;
        }

        .fc-event-time {
            font-size: 0.75em !important;
            opacity: 0.95 !important;
            font-weight: 500 !important;
            color: white !important;
        }

        /* Fix for event content visibility */
        .fc-event-main {
            color: white !important;
        }

        .fc-event-main-frame {
            color: white !important;
        }

        .fc-daygrid-event-harness {
            margin: 2px 3px !important;
        }

        .fc-daygrid-event {
            border-radius: 6px !important;
            font-size: 0.85em !important;
        }

        /* Event Details Modal */
        .modal {
            display: none;
            position: fixed;
            z-index: 1000;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(61, 41, 20, 0.7);
            backdrop-filter: blur(5px);
        }

        .modal-content {
            background: linear-gradient(145deg, var(--cream-white), var(--sandalwood-light));
            margin: 5% auto;
            padding: 30px;
            border: 3px solid var(--royal-gold);
            border-radius: 20px;
            width: 90%;
            max-width: 600px;
            position: relative;
            box-shadow: 0 20px 60px rgba(61, 41, 20, 0.3);
            font-family: var(--font-classic);
            animation: modalSlideIn 0.3s ease-out;
        }

        @keyframes modalSlideIn {
            from {
                opacity: 0;
                transform: translateY(-50px) scale(0.9);
            }
            to {
                opacity: 1;
                transform: translateY(0) scale(1);
            }
        }

        .modal-header {
            border-bottom: 2px solid var(--royal-gold);
            padding-bottom: 15px;
            margin-bottom: 20px;
        }

        .modal-title {
            font-family: var(--font-display);
            font-size: 2em;
            color: var(--chocolate-brown);
            margin: 0;
            text-align: center;
        }

        .modal-body {
            line-height: 1.8;
            color: var(--rich-brown);
        }

        .modal-body p {
            margin-bottom: 15px;
            font-size: 1.1em;
        }

        .modal-body strong {
            color: var(--chocolate-brown);
            font-family: var(--font-display);
            font-weight: 600;
        }

        .modal-body .description {
            background: rgba(245, 222, 179, 0.3);
            padding: 15px;
            border-radius: 10px;
            border-left: 4px solid var(--royal-gold);
            margin-top: 20px;
            font-style: italic;
        }

        .close {
            position: absolute;
            top: 15px;
            right: 20px;
            color: var(--chocolate-brown);
            font-size: 32px;
            font-weight: bold;
            cursor: pointer;
            transition: color 0.3s ease;
        }

        .close:hover {
            color: var(--royal-gold);
            transform: scale(1.1);
        }

        .artist-info {
            background: linear-gradient(135deg, var(--chocolate-brown), var(--rich-brown));
            color: white;
            padding: 10px 20px;
            border-radius: 25px;
            display: inline-block;
            margin-bottom: 15px;
            font-weight: 600;
            box-shadow: 0 4px 15px rgba(61, 41, 20, 0.3);
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .page-title {
                font-size: 2.2em;
                margin: 30px 20px;
            }

            #calendar {
                margin: 0 20px 40px;
                padding: 20px;
            }

            .modal-content {
                margin: 10% auto;
                padding: 20px;
                width: 95%;
            }

            .modal-title {
                font-size: 1.6em;
            }

            .fc-toolbar {
                flex-direction: column !important;
                gap: 10px !important;
            }

            .fc-toolbar-chunk {
                display: flex !important;
                justify-content: center !important;
            }

            .fc-daygrid-day {
                min-height: 80px !important;
            }
        }

        @media (max-width: 480px) {
            .page-title {
                font-size: 1.8em;
            }

            #calendar {
                padding: 15px;
            }

            .modal-content {
                padding: 15px;
            }

            .fc-button {
                padding: 6px 12px !important;
                font-size: 0.85em !important;
            }

            .fc-daygrid-day {
                min-height: 60px !important;
            }

            .fc-daygrid-day-number {
                font-size: 1em !important;
            }
        }
    </style>
</head>
<body>
    <%- include('partials/navbar') %>

    <h1 class="page-title">Workshops & Events Calendar</h1>
    <div id='calendar'></div>

    <!-- Event Details Modal -->
    <div id="eventModal" class="modal">
        <div class="modal-content">
            <span class="close">&times;</span>
            <div class="modal-header">
                <h2 class="modal-title" id="modalTitle"></h2>
            </div>
            <div class="modal-body" id="modalBody">
                <!-- Event details will be populated here -->
            </div>
        </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            var calendarEl = document.getElementById('calendar');
            var modal = document.getElementById('eventModal');
            var span = document.getElementsByClassName('close')[0];

            var calendar = new FullCalendar.Calendar(calendarEl, {
                initialView: 'dayGridMonth',
                headerToolbar: {
                    left: 'prev,next today',
                    center: 'title',
                    right: 'dayGridMonth,timeGridWeek,listWeek'
                },
                height: 'auto',
                events: '/api/workshops',
                eventDidMount: function(info) {
                    // Add proper time display to events
                    const props = info.event.extendedProps;
                    if (props.startTime && props.endTime) {
                        const timeElement = info.el.querySelector('.fc-event-time');
                        if (timeElement) {
                            timeElement.textContent = `${props.startTime} - ${props.endTime}`;
                        }
                    }
                    
                    // Ensure text is visible
                    const titleElement = info.el.querySelector('.fc-event-title');
                    if (titleElement) {
                        titleElement.style.color = 'white';
                        titleElement.style.textShadow = '1px 1px 2px rgba(0,0,0,0.7)';
                    }
                },
                eventClick: function(info) {
                    // Show event details in modal
                    const event = info.event;
                    const props = event.extendedProps;
                    
                    document.getElementById('modalTitle').textContent = event.title;
                    
                    const modalBody = document.getElementById('modalBody');
                    modalBody.innerHTML = `
                        <div class="artist-info">By: ${props.artistName || 'Unknown Artist'}</div>
                        <p><strong>📅 Date:</strong> ${event.start.toLocaleDateString('en-US', { 
                            weekday: 'long', 
                            year: 'numeric', 
                            month: 'long', 
                            day: 'numeric' 
                        })}</p>
                        <p><strong>🕐 Time:</strong> ${props.startTime || 'TBA'} - ${props.endTime || 'TBA'}</p>
                        <p><strong>📍 Location:</strong> ${props.location || 'TBA'}</p>
                        <p><strong>💰 Price:</strong> $${props.price || '0'}</p>
                        <p><strong>🎨 Category:</strong> ${props.category || 'Workshop'}</p>
                        <div class="description">
                            <strong>Description:</strong><br>
                            ${props.description || 'No description available.'}
                        </div>
                    `;
                    
                    modal.style.display = 'block';
                    
                    // Prevent the default FullCalendar behavior
                    info.jsEvent.preventDefault();
                }
            });

            calendar.render();

            // Modal close functionality
            span.onclick = function() {
                modal.style.display = 'none';
            }

            window.onclick = function(event) {
                if (event.target == modal) {
                    modal.style.display = 'none';
                }
            }
        });
    </script>
</body>
</html>